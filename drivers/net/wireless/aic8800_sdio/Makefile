CONFIG_AIC8800_BTLPM_SUPPORT := m
CONFIG_AIC8800_WLAN_SUPPORT := m
CONFIG_AIC_WLAN_SUPPORT := m

obj-$(CONFIG_AIC8800_BTLPM_SUPPORT) += btlpm/
obj-$(CONFIG_AIC8800_WLAN_SUPPORT) += fdrv/
obj-$(CONFIG_AIC_WLAN_SUPPORT) += bsp/

CONFIG_PLATFORM_ROCKCHIP = n
CONFIG_PLATFORM_ROCKCHIP2 = y

ifeq ($(CONFIG_PLATFORM_ROCKCHIP), y)
	ccflags-y += -DCONFIG_PLATFORM_ROCKCHIP
endif

ifeq ($(CONFIG_PLATFORM_ROCKCHIP2), y)
	ccflags-y += -DCONFIG_PLATFORM_ROCKCHIP2
endif
